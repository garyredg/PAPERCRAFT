## 2026-09-02
- Camera-relative movement: WASD and the controller left stick are now rotated by the client-local camera yaw before their world-space vector is sent to the authoritative server, so forward and strafing follow the current view rather than map axes. The server's movement and collision authority are unchanged. (sess-20260825-1938-f6bd411e)

## 2026-08-30
- New docs/NORTHSTAR_EMILYOS_UI.md: real scoping pass bringing EmilyOS's own UX (tmux x i3 hybrid, EGSHELL, double-click-speed contract) into PAPERCRAFT as an in-game UI layer -- per-account workspaces, notes mod (real, this pass), bash mod (scoped, deferred pending real security design). New packages/simulation/notes_mod.c + notes_mod_test.c, 8 assertions pass. (sess-20260825-1938-f6bd411e)
- New docs/NORTHSTAR_WEB_CLIENT.md -- real scoping pass for a native TS+WebGL browser client (no Emscripten), WebRTC DataChannel bridge, no code yet (sess-20260825-1938-f6bd411e)
- Halved snapshot broadcast bandwidth (PC_SNAPSHOT_HZ=10, decoupled from 20Hz sim tick) to reduce reconnection flashing on low-bandwidth connections; bumped PC_CLIENT_WEAK_MS; fixed a live var/players+var/world permission bug that was silently failing player autosaves (sess-20260825-1938-f6bd411e)
- Entity + inventory system: GTA3-style item drops (PC_PACKET_ENTITY_SPAWN/DESPAWN), FFXI-style list inventory UI (PC_PACKET_INVENTORY_UPDATE), 3 new PARENA mods (item_drop/inventory/pickup), SDL_GameController support; native cc_test replacing an earlier live-probe verification (sess-20260825-1938-f6bd411e)
- TYLER phone mechanics Phase 1: PARENA phone_mod + PC_PACKET_PHONE_MESSAGE wire protocol + client notification banner; also fixed a real spawn_player latest_cmd_seq reset bug found during live verification (sess-20260825-1938-f6bd411e)
- S206-59: more forgiving connection-loss thresholds for low bandwidth. Founder real-time (after confirming the full v0.12.0 pipeline -- login, movement, mouse-look, ping -- works over real 5G): "we are getting a lot of connection lost can we get it to be more forgiving for low bandwidth?" Real, coupled fix -- both thresholds bumped together, kept proportional: apps/server's own PC_PLAYER_TIMEOUT_MS 30000 -> 60000ms, apps/client's own PC_CLIENT_STALE_MS (the disruptive full-screen takeover) 20000 -> 45000ms, still a real 15s safety margin under the server's own new eviction window. The short, non-disruptive PC_CLIENT_WEAK_MS (2000ms) indicator is unchanged. README.md/NORTHSTAR.md updated (stale documented numbers). v0.14.0 released. Live production server binary rebuilt in place; restart queued (41-restart-papercraft-server-for-low-bandwidth-thresholds.sh). (sess-20260825-1938-f6bd411e)
- S206-58: real ping/RTT counter, closing the founder's original "show the ping at the top of the screen" ask in full. PcSnapshotPacket gains one echo_cmd_time_ms field (not a per-player array -- a client only ever needs its own value), overwritten per-recipient by apps/server right before each player's own sendto() with that player's own last-sent PcUserCmdPacket::cmd_time_ms. Client computes now_ms() - echo_cmd_time_ms for a real round-trip time with zero clock-sync assumption (both send and compare happen on the same client's own clock). Displayed top-right with green/yellow/red banding at 100ms/300ms, alongside the existing top-left HUD and top-center weak-connection indicator. Wire cost: 1432 -> 1436 bytes, still 36 bytes under budget. Verified live end to end: a real throwaway server + UDP probe sent a real PcUserCmdPacket with a known cmd_time_ms and confirmed the echoed value matched exactly, 8ms measured RTT on localhost. v0.12.0 released. Live production server binary rebuilt in place; restart queued (40-restart-papercraft-server-for-ping-feature.sh) since the running instance predates this protocol field. (sess-20260825-1938-f6bd411e)

- S206-57: real mouse-look camera. Founder real-time: "we can log in but there is no interraction im expecting to be able to move and look around". Real, confirmed finding: this client never had any camera control beyond movement direction -- the eye position was hardcoded directly behind own.yaw (server-authoritative, movement-derived facing). Fixed with real, standard mouse-look: SDL_SetRelativeMouseMode captures the cursor after login; purely client-local cam_yaw/cam_pitch state drives a real spherical orbit camera, decoupled from the player's own rendered facing (still server-authoritative, unaffected by looking around). Orbit radius/pitch hand-derived (not guessed) to reproduce the exact old camera position on the first frame -- a real bug (measuring vertical offset from own.y instead of the real look-at target own.y+1.0) was caught and fixed before shipping, verified via a Python hand-trace showing zero floating-point difference. v0.10.0 released. (sess-20260825-1938-f6bd411e)

## 2026-08-29
- S206-54 closed: PAPERCRAFT's first real live player, end to end. Real live-service deployment (papercraft-server.service systemd unit, PAPERCRAFT_TICKET_SECRET added to IDUNA, IDUNA rebuilt in place after finding the deployed binary predated the papercraft ticket handler's own commit), a firewall opened for 7799/udp+7070/tcp+8080/tcp (verified externally via check-host.net from multiple regions), PLAY_ONLINE.bat/.sh (real public host flags baked in via okemily.com), papercraft_client.log (file-based logging, necessary since PLAY_ONLINE's console closes on exit), verbose [http] diagnostic logging in http_client.h (both Windows and POSIX branches), and the real bug that logging found: a genuine WSAStartup ordering bug (WSA error 10093) that had nothing to do with the extensive firewall/DNS investigation that preceded it. Then a real design flaw found from live play on the founder's own 5G connection: movement input was gated on the same flag that flips false during any connection hiccup -- redesigned to a three-tier model (continuous input via a sticky ever_welcomed flag, a small non-disruptive weak-connection indicator, a 20s last-resort full reconnect). v0.1.0 through v0.8.0 shipped across this arc. (sess-20260825-1938-f6bd411e)
- S206-53 closed: real cross-platform CI now succeeds end to end. Fixed the bazel test job (bare "bazel test //..." was trying to build SDL2/GL-dependent binaries the bare CI runner had no dev headers for -- fixed with --build_tests_only). Spent five failed runs debugging a macOS build that never got past the client compile step, surfacing three real GitHub Actions gotchas along the way (sdl2-config PATH assumptions, set -u silently killing a diagnostic wrapper via cross-step $GITHUB_ENV propagation, and finally GitHub's own implicit "-e" on every shell:bash step swallowing captured diagnostics) before the founder made the real call to drop macOS entirely rather than keep burning cycles on an unverifiable target. Repaired the dangling build_macos references the founder's own removal commit left behind. Final run (0de8563) passed all four jobs and cut a real first GitHub Release: v0.1.0, with papercraft-linux-x86_64.tar.gz and papercraft-windows-x86_64.zip as real downloadable assets. (sess-20260825-1938-f6bd411e)
- Real cross-platform CI: new .github/workflows/ci.yml builds apps/client + apps/mapeditor for Linux (native gcc), Windows (mingw-w64 cross-compile), and macOS (native clang, Homebrew SDL2), uploading each as a GitHub Actions artifact, gated behind a real bazel test //... run. apps/server stays Linux-only, excluded by design. Fixed 3 real cross-platform bugs surfaced along the way: a Windows include-order bug (winsock2.h vs GL/gl.h's own windows.h), a Windows mkdir() argument-count compile error in packages/common/papercraft_worldobjects.h (new PC_MKDIR macro), and a macOS GL header path gap (OpenGL/gl.h vs GL/gl.h). Verified via real local raw-gcc and raw-mingw-gcc dry-runs matching the workflow's exact commands, plus a full clean bazel build/test (27 targets, 12/12 tests pass). macOS job itself unverifiable locally (no macOS host in this environment) -- honestly flagged in NORTHSTAR.md/README.md. (sess-20260825-1938-f6bd411e)
- Phase 1c-client: apps/client now renders the real server-authoritative fragment rotation Phase 1c broadcasts. Shared lookup renamed pc_falling_lookup_y -> pc_falling_lookup (returns whole PcFallingFragment, both y and rotation_deg, in one scan). Rotation applied by hand per-vertex around each debris piece's own local centroid in the XZ plane, matched pieces only; unmatched pieces unchanged. Test coverage extended to 11 assertions (was 8), 3 new covering rotation_deg. Verified via hand-traced numeric rotation example, raw-gcc compile checks, and a full clean bazel build/test (27 targets, 12/12 tests pass). Closes the client-rendering gap Phase 1c (S206-50) explicitly deferred. (sess-20260825-1938-f6bd411e)
- Real Phase 1c shipped server-side: falling fragments now spin at a real, deterministic constant rate (rotation_deg on PcFallingFragment, 90-270 deg/s derived from fragment_idx), verified live via a real UDP probe matching the hand-computed expected rate (167.1429 vs observed 167.1428 deg/s, ratio 1.000); wire cost measured at 1432 bytes, still 40 under budget; client rendering deliberately deferred (no live Xvfb verification available), matching Phase 1a/1b's own honest scope pattern (sess-20260825-1938-f6bd411e)
- Closed out S206-47's own explicitly-named multi-fragment verification gap: traced the earlier inconclusive result to two real root causes (a probe facing-pulse that overshot position, and a first fragment pair that turned out geometrically unreachable together from ground level), retried with a correctly-chosen pair, confirmed the bounded falling-fragment array correctly tracks two simultaneous detach events in separate slots; no code changed, verification only (sess-20260825-1938-f6bd411e)
- Real Phase 1b shipped: apps/client now renders the server-authoritative fragment fall from Phase 1a's wire data, replacing local vertical simulation for a tracked piece while keeping its existing cosmetic horizontal scatter (a deliberate split, not a full replacement, to avoid a visual regression); new pure, testable pc_falling_lookup_y with 8 real assertions in a new packages/common cc_test; verified via clean compilation + hand-traced coordinate math, no live Xvfb session (no test IDUNA user available) (sess-20260825-1938-f6bd411e)
- Real Phase 1a shipped: server-authoritative fragment physics, the smallest first slice from NORTHSTAR.md's own DoD -- a small bounded set of just-detached fragments fall under real gravity and land at the real ground height server-side, broadcast over a deliberately minimal wire shape (only y, not full x/y/z); verified live end-to-end via a real UDP probe reading the new packet fields directly, watching real y decrease under gravity then land; client rendering deferred as Phase 1b (sess-20260825-1938-f6bd411e)
- NORTHSTAR.md: real Phase 1 scoping section added, naming server-authoritative fragment physics as the deliberately-chosen next target (over the embedded PARENA editor, real weapon/combat, and full-city conversion, since it needs no founder product direction or wire-streaming redesign first) with a real, narrow Definition of Done for its own first slice, matching PARENA VS0's own DoD precedent -- docs-only, no code changed (sess-20260825-1938-f6bd411e)
- README.md refreshed: fixed a real stale test count (was 8, now correctly 11 after S206-37/38), added scripts/build-parena.sh as the recommended build one-liner, added mapeditor remove to Quick Start, added abandoned-connection handling and the progression HUD to the feature list, mentioned --mods-manifest dynamic mod loading, corrected the embedded-PARENA-editor and full-city-destructibility Not yet real bullets (sess-20260825-1938-f6bd411e)
- PC_WO_MAX_OBJECTS doubled from 4 to 8 (direct consequence of S206-43's bit-packing win, sizeof(PcSnapshotPacket) now 1380 bytes, still 92 under budget); Wall B now gets its own precise two-box L-shape matching Wall A's, closing the last-remaining approximation gap; verified live, real carve-out removes exactly 30 blocks matching worldapi data for both walls (sess-20260825-1938-f6bd411e)
- Bit-packed PcSnapshotPacket's world_object_state (2 bits/fragment instead of a full byte), cutting sizeof(PcSnapshotPacket) from 1408 to 1120 bytes -- a real 288-byte wire-budget win, a narrow first slice of the full-city-conversion wire-budget limit; verified live end-to-end with a real UDP probe using the new pc_wo_state_unpack accessor, confirming correct fragment-state transitions and XP award through the actual packed wire format (sess-20260825-1938-f6bd411e)
- mapeditor add/edit now reject a zero or negative half-extent (previously silently accepted, producing a degenerate invisible object) with a clear, specific error naming the rejected flag and value; verified live across 4 real scenarios (sess-20260825-1938-f6bd411e)
- mapeditor remove now automatically re-syncs the damage file's own rows to match a real object shift, instead of just warning about the desync -- closes the index-desync gap completely for everything the toolset can do (remove is the only operation that reindexes at all); verified live with distinct per-object damage markers correctly following their objects to the new indices (sess-20260825-1938-f6bd411e)
- Real abandoned-connection handling: apps/server frees a slot (with a real final autosave) after 30s of silence from a client, fixing a real slot-leak on this always-running persistent server; apps/client detects 5s of no snapshot traffic and shows a real CONNECTION LOST -- RECONNECTING screen, re-triggering its existing connect-retry loop; verified live with a real 31-second real-time UDP probe test on the server side; NORTHSTAR.md updated (sess-20260825-1938-f6bd411e)
- Wall A is now a real, precise two-box L-shape (PC_CITY_WALL_A1/A2) matching its exact real 15-block footprint, re-confirmed live against the actual worldapi -- closes part of docs/NORTHSTAR_PAPER_ENGINE.md's own L-shaped/multi-part object gap; Wall B stays a bounding-box approximation, PC_WO_MAX_OBJECTS=4 now fully used; verified live via real carve-out block counts (10+5=15) (sess-20260825-1938-f6bd411e)
- Added real round-trip + failure-mode test coverage for PcSaveRecord (papercraft_persist_test.c) and both PcWorldObjectFile/PcWorldDamageFile (papercraft_worldobjects_test.c), closing previously-invisible zero-coverage gaps for two real save-file formats verified only by hand until now; new bazel cc_test targets, all green under a fully clean rebuild (26 targets, 11/11 tests) (sess-20260825-1938-f6bd411e)
- Added real RFC 4231 test-vector coverage for hmac_sha256.h (packages/common/hmac_sha256_test.c, ported from shankpit-460's own never-built-into-a-CI test, plus new coverage for hmac_sha256_verify that original never had) wired into a real bazel test target for the first time in this repo lineage -- a security-critical connect-ticket primitive with zero prior automated coverage (sess-20260825-1938-f6bd411e)
- NORTHSTAR.md: fixed three stale claims in the talent/ability-point bones section -- game-loop integration and the real MOVE stat effect were both already shipped, and a real UI (draw_progression_hud, live level/XP/points/ability readout) already exists and was wrongly claimed missing; caught before committing by reading apps/client's actual source (sess-20260825-1938-f6bd411e)
- Fixed three stale top-of-file comments (apps/server/src/main.c, packages/common/papercraft_world.h, packages/common/papercraft_persist.h) that no longer matched shipped code (jump/destruction/talent claimed missing, multi-chunk grid and world-damage persistence claimed not built); zero behavior change, verified via a full clean rebuild (sess-20260825-1938-f6bd411e)
- mapeditor remove now warns (doesn't block) when removing a non-last object would shift others down an index while a real damage file already exists -- apps/server restores damage by index, so a shift would silently misattribute damage state; new --damage-file flag; verified live across 4 real scenarios; papercraft_worldobjects.h and NORTHSTAR.md updated (sess-20260825-1938-f6bd411e)
- mapeditor: added a real edit command for in-place object field edits (position/material/seed/extents), preserving the object's own index and carve bounds -- previously remove+add would silently break any per-object damage state keyed by index; verified live including overlap-warning and error paths; README.md and NORTHSTAR.md updated (sess-20260825-1938-f6bd411e)
- apps/server: real SIGHUP live-reload for the dynamically-loaded mods manifest (dlcloses+reopens every .so so a rebuilt mod is actually picked up, no restart), verified live growing/shrinking the manifest and a clean no-op when --mods-manifest was never given; MODDING.md and NORTHSTAR.md updated (sess-20260825-1938-f6bd411e)
- Added scripts/build-parena.sh: finds a sibling PARENA checkout (or PARENA_DIR), runs make build, prints the resolved ./parena binary path, with real actionable errors for a missing/malformed checkout; closes the friction in MODDING.md's own Two repos, not one gap without vendoring a binary (sess-20260825-1938-f6bd411e)
- Wired the real xp_award call site (destroyed-world-object event) to prefer a dynamically-loaded mod over the statically-linked one via mod_registry_lookup, falling back cleanly when unregistered; verified live end-to-end via a real UDP probe against two throwaway server instances, identical +60 XP reward through both real code paths; MODDING.md and NORTHSTAR.md updated (sess-20260825-1938-f6bd411e)
- apps/server gains an optional --mods-manifest flag: real dlopen/dlsym mod loading at startup into an in-memory registry, default off, with a designed error-handling policy (bad mod = WARNING + skip, never fatal); new testdata/mods_manifest.example.txt; verified live against a throwaway server instance including a deliberately-broken manifest; MODDING.md and NORTHSTAR.md updated (sess-20260825-1938-f6bd411e)
- Real proof of the Bool mod-return shape: new libtalent_mod.so (VS0 compiles Bool to the same plain int ABI as I32, zero new dynmod_poc dispatch code needed); 3 real hand-traced cases verified, 3 distinct .so files now proven loaded together in one process via the manifest mode; MODDING.md and NORTHSTAR.md updated (sess-20260825-1938-f6bd411e)
- Added real multi-mod manifest mode to apps/dynmod_poc (so_path|function|expected[|arg1[|arg2]]), proving distinct PARENA-compiled mods (libxp_award_mod.so, liblevel_mod.so) load and run together in one process; refactored shared dlsym/dispatch logic; new testdata/manifest.txt fixture; MODDING.md and NORTHSTAR.md updated (sess-20260825-1938-f6bd411e)
- Generalized apps/dynmod_poc dynamic mod-loading proof to real 0/1/2-arg I32 shapes (xp_award_mod, level_mod xp_required_for_level, level_mod on_papercraft_level_for_xp), including real recursion inside a dynamically-loaded .so; new liblevel_mod.so Bazel target; MODDING.md and NORTHSTAR.md updated (sess-20260825-1938-f6bd411e)
- Real proof-of-concept for dlopen-based mod loading (apps/dynmod_poc): a real, standalone tool dlopens/dlsyms a real PARENA-compiled mod function built as a shared library, proving the mechanism works with zero mod-file changes and zero host rebuild. Found and fixed a real Bazel gotcha along the way (alwayslink needed to preserve symbols across --start-lib/--end-lib archive resolution). Verified fully clean (bazel clean + plain build/test, no special flags): 21 targets, 8/8 mod tests, real regression check on apps/server passes. Commit 5bb7696. (sess-20260825-1938-f6bd411e)
- Real per-face subdiv scaling for non-uniform Paper Engine boxes: paper_face_grid picks the best-fit (gu,gv) grid per face by real aspect ratio, keeping the total fragment count (and wire format) unchanged. Verified with direct assertions (3:1 aspect -> 8x2, provably closer than naive 4x4) and a live interact/damage regression check on a real carved city wall. bazel build/test //... clean. Commit 27b67ba. (sess-20260825-1938-f6bd411e)
- Resolved the real open WEAKNIGHT_BEDROCK_RACERS map-editor question NORTHSTAR.md itself named: checked EMILY/BACKLOG.md S204-08 directly, confirmed it's still entirely unscoped/un-built there -- no existing tool to reconcile with. Also verified README.md's own Quick Start section literally against a real bazel build, zero discrepancies. Commit 5cd7451. (sess-20260825-1938-f6bd411e)
- Real top-level README.md: pitch, a cross-checked 'what's real today' summary, verified quick-start build/run commands, verified real controls, a docs index, and an honest architecture overview. Closes a genuine discoverability gap -- this repo had 22 real shipped features and zero README before now. Commit 92fa6aa. (sess-20260825-1938-f6bd411e)
- Real AABB overlap detection in apps/mapeditor's add command -- warns (doesn't block) if a new object's bounding box overlaps an existing one's. Verified live: overlapping placement correctly warned naming the colliding object; non-overlapping and near-miss-boundary placements correctly produced no false positive. bazel build/test //... clean. Commit ef38512. (sess-20260825-1938-f6bd411e)
- Real, general, data-driven carve-out system: PcWorldObjectDef grew has_carve/carve_x0..carve_z1 fields any object can use, apps/mapeditor grew --carve. Proved with a real second carved wall (30 real blocks removed total across both). Caught and fixed a real wire-budget regression (1488 bytes, packed to unsigned char, landed at 1408) and two */ comment-terminator bugs live. Verified end-to-end through the actual mapeditor CLI, not just internal seeding. bazel build/test //... clean (19 targets, 8/8 mod tests). Commit 0e90a8c. (sess-20260825-1938-f6bd411e)
- Real first case of VoxelBlock city-wall integration: carved a real, confirmed-live ~15-block wall structure out of the city's normal render/collision path, replaced with a real non-cube Paper Engine object sized to its exact bounding box. Verified live -- server confirmed exact block-removal count, real UDP probe confirmed damage registers on the carved-out object. bazel build/test //... clean (19 targets, 8/8 mod tests). Commit 12aaa56. (sess-20260825-1938-f6bd411e)

- Real interact damage falloff by distance: new PARENA mod (on-papercraft-interact-damage-falloff), composed with the existing material-resistance decision. Verified live -- the exact same 96-fragment PAPER object one-shot scenario (73/96 GONE before) now produces a real gradient (52 intact, 23 cracked, 11 torn, 10 gone), server log matching exactly. bazel build/test //... clean (19 targets, 8/8 mod tests). Commit ab65d43. (sess-20260825-1938-f6bd411e)

## 2026-08-28
- Real non-cube base shapes: paper_generate_box takes independent per-axis half-extents, apps/mapeditor grew --half-x/--half-y/--half-z. Verified live -- placed a real wall (half=3.0,1.5,0.15), confirmed server broadcast the exact shape, confirmed interact/damage still works on the asymmetric object, screenshot showed a genuinely wide/short slab, not a cube. bazel build/test //... clean (17 targets, 7/7 mod tests). Commit b5cb431. (sess-20260825-1938-f6bd411e)
- Real client-side fragment debris on destruction: diffs consecutive snapshots for real GONE transitions, spawns a debris piece using the fragment's own real jittered geometry, real gravity + fade. Verified server-side (73 fragments broke off in one hit); screenshot verification of debris-in-flight was attempted but honestly noted as incomplete (small/fast pieces, no xdotool available). Bonus: confirmed real 2-player simultaneous rendering for the first time this session. bazel build/test //... clean. Commit 283b775. (sess-20260825-1938-f6bd411e)
- Real world-object damage persists across a restart: new PcWorldDamageFile persists per-fragment hp, restored via the same real PARENA-compiled state-derivation function fresh damage uses. Verified live -- punched an object twice, SIGTERM, restarted, reconnected: checksum came back byte-identical (3519267552, 6/96 non-intact). bazel build/test //... clean (17 targets, 7/7 mod tests). Commit 543bd4d. (sess-20260825-1938-f6bd411e)
- Real MODDING.md, verified end-to-end: a real, step-by-step guide (build parena -> write .prn -> compile -> wire into host -> bazel build -> run server -> connect client), written and confirmed live with a brand-new mod (xp_award_mod, real bonus XP for destroying a Paper Engine object, ported from the construct's own real per-kill XP value). Server log confirmed 'destroyed world object 0 -- +60 real xp_award_mod XP'. bazel build/test //... clean (17 targets, 7/7 mod tests). Commit 66bf592. (sess-20260825-1938-f6bd411e)
- Real map editor, first pass: apps/mapeditor (offline CLI add/list/remove) places real Paper Engine props into a real, persisted world-objects file apps/server loads at startup. PcSnapshotPacket now broadcasts a real, bounded multi-object list (up to 4). Verified live -- placed 3 real objects, confirmed per-object interact targeting (only the punched object takes damage). bazel build/test //... clean (15 targets, 6/6 mod tests). Commit 14d7e82. (sess-20260825-1938-f6bd411e)
- Real jump physics + first real trick input (slide-jump, ported from SHANKPIT_CONSTRUCT.txt's own 'PHASE 485: TUNED SLIDE JUMP'). Verified live -- server log confirmed 'Player slot 0 landed a real slide-jump trick -- 1.06x speed for 800ms' at cruise speed, matching the unit test's own independently computed value. bazel build/test //... clean (14 targets, 6/6 mod tests). Commit 8865298. (sess-20260825-1938-f6bd411e)
- Real player persistence across a server restart: PcSaveRecord flat-binary save keyed by player_id, periodic autosave (10s) + SIGINT/SIGTERM graceful-save. Verified live -- level/xp/talent/position all survived a real SIGTERM+restart cycle. bazel build/test //... clean. Commit 4e05936. (sess-20260825-1938-f6bd411e)
- Real multi-chunk city traversal: a fixed 3x3 worldapi chunk grid (9 chunks) replaces the old hardcoded single (cx=0,cz=0) chunk. Verified live via UDP probe -- player walked x=8 to x=30 across the real chunk boundary with ground height correctly resolving the whole way. bazel build/test //... clean. Commit 4e77fbd. (sess-20260825-1938-f6bd411e)
- Wired the Paper Engine (paper_fragment_mod) into the live game loop: real PC_PACKET_INTERACT + a world-positioned 96-fragment concrete test cube, verified end-to-end via UDP probe (5 real fragments broke INTACT->CRACKED->GONE). bazel build/test //... clean, 12 targets, 5 mod tests passing. Commit 0fc1021. (sess-20260825-1938-f6bd411e)
- Real MOVE-stat gameplay effect: new stat_effects_mod.prn ports the construct's own progression_apply_bonuses formula (boost = 1.0 + 0.035*rank) as I32 permille, wired into apps/server's real movement tick. Verified live at exactly 1.0350x measured speed ratio, matching the real construct formula precisely. Other 4 stats deliberately deferred (no health/shield/cooldown systems exist yet). (sess-20260825-1938-f6bd411e)
- Wired talent_mod.c into the live game loop: real PC_PACKET_ALLOCATE_TALENT (client keys 1-5) -> server calls the real PARENA-compiled on_papercraft_can_allocate_talent gate -> applies ability++/points-- if legal. Real HUD readout when points are available. Verified live end-to-end via UDP probe (real level-up granted a point, real allocation applied it). (sess-20260825-1938-f6bd411e)
- Wired the already-tested level_mod.c into the live game loop: real per-second XP tick (server), real level-up decisions via the real PARENA-compiled function, real HUD readout (client, the exact construct HUD line format). Found and fixed a real 512-byte snapshot buffer overflow bug live (compiler -Warray-bounds caught it). Verified live: a real level-up at exactly 80 cumulative XP. (sess-20260825-1938-f6bd411e)
- Real Phase 0 client (apps/client): real login screen -> ticket mint (no queue) -> UDP connect -> renders the real live worldapi city chunk as real cubes with a real chase camera on the server's own snapshot position. Verified visually via a real Xvfb screenshot (fixed a real 3-of-6-faces player marker bug along the way). Phase 0's full login+spawn+movement+render loop is now real end to end. (sess-20260825-1938-f6bd411e)
- Real Phase 0 server (apps/server): fetches the real live worldapi city chunk (scene 200, 1054 blocks), verifies real IDUNA connect tickets, spawns a player on the real ground height, ticks real server-authoritative on-foot movement with basic collision. Found and fixed a real bug: http_client.h's shared 8KB response buffer silently truncated the real ~35KB chunk response. Verified live end-to-end via UDP probe. No client yet. (sess-20260825-1938-f6bd411e)
- Real talent-point allocation gate (packages/simulation/talent_mod.c, from PARENA/stdlib/papercraft/talent_mod.prn) — direct port of the real construct's own progression_try_allocate (cap of 5, unspent-points gate). bazel test //... 4/4 pass. (sess-20260825-1938-f6bd411e)
- The Paper Engine: real destructible-geometry proof of concept — subdivide-a-cube-and-jitter-vertices mesh generator (packages/common/paper_mesh.h, deterministic/seeded), real PARENA-compiled damage-decision mods (material-aware resistance + threshold-based damage tiers). Verified: 96 fragments from a 4x4 cube, real seed-determinism, real local shotgun-blast-opens-a-hole scenario (4/96 fragments detached). bazel test //... 3/3 pass. docs/NORTHSTAR_PAPER_ENGINE.md has the full design + a long list of flagged future mechanics (wet-concrete transfer, worker rebuild events, fractal/math-heavy simulation). (sess-20260825-1938-f6bd411e)
- Fixed level_mod.prn to use the real construct XP curve (80 + (level-1)*35, from SHANKPIT_CONSTRUCT.txt's own MatchProgression) instead of an invented one. NORTHSTAR.md gains talent-tree/map-editor/full-PARENA-toolchain vision notes, all flagged not built. (sess-20260825-1938-f6bd411e)

- New repo: NORTHSTAR.md (Minecraft meets GTA3 plus Skate2 / Skyrim meets Cyberpunk, non-voxel, single-node persistent online-only sandbox MMO, mods-first PARENA) + first real PARENA mod (level_mod.prn -> level_mod.c, real recursive RPG level-for-XP decision logic, bazel test green) + real Bazel build from day one. (sess-20260825-1938-f6bd411e)
